{
    "builds": [
        {
            "src": "./webapp.py",
            "use": "@vercel/python"
            "config": { "runtime": "python3.6" }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}
