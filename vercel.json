{
    "builds": [
        {
            "src": "./webapp.py",
            "use": "@vercel/python"
            "config": { "runtime": "python3.8" }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}
